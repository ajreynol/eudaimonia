module

-- This file is hand-written. A signature install leaves it alone, so what is
-- put here is kept.
--
-- It is a stub: what stands here is a description of what belongs in it.
--
-- The soundness theorem of the checker: that a proof it accepts implies its
-- assumptions are unsatisfiable, under the side conditions of `Assumptions`.
--
-- `CheckerCore` supplies the rule-agnostic half and `RuleLemmas` the per-rule
-- half; this is where they are put together.
--
-- In Logos: `Cpc/Proofs/Checker.lean`, whose `correct___eo_is_refutation` this
-- corresponds to.

public import @CALCULUS@.Proofs.RuleLemmas
import all @CALCULUS@.Proofs.RuleLemmas

public section
