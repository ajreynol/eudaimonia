module

-- **GENERATED.** You will not need to edit this file, and must not: installing
-- a signature overwrites it. Change the signature, or its semantics, instead.
--
-- Until one is installed this is a placeholder describing what will stand here.
--
-- The term datatype of the calculus: one constructor per operator of the
-- signature, and the operations on terms the proof rules are written over.
--
-- This is the deep embedding the whole package is stated about. It is compiled
-- from the signature, so adding an operator to the signature adds a constructor
-- here.
--
-- In Logos: `Cpc/LogosTerm.lean`.

public import @CALCULUS@.SmtEval
import all @CALCULUS@.SmtEval

public section
