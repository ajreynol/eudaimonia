module

-- **GENERATED.** You will not need to edit this file, and must not: installing
-- a signature overwrites it. Change the signature, or its semantics, instead.
--
-- Until one is installed this is a placeholder describing what will stand here.
--
-- The primitives the deep embedding is evaluated over: the native operations on
-- numerals, rationals, strings and bit-vectors that both the checker and the
-- semantics are written in terms of.
--
-- The base of the package -- it imports nothing but `Init`, and everything else
-- imports it.
--
-- In Logos: `Cpc/SmtEval.lean`.

public section
